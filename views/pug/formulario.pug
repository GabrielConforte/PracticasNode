html 
    head    
        title Formulario desde Pug
        link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css')
    body
        div(class="container mt-2 d-flex flex-column align-items-center")
            h1 Turbo Pug
            form(class="prod-form" action='/productos', method='POST')
                p
                | Nombre
                input(class="form-control" type='text', name='title', value='')
                | Precio
                input(class="form-control" type='number', name='price', value='')
                | URL
                input(class="form-control" type='text', name='url', value='')
                input(class="btn btn-primary mt-2" type='submit', value='Submit')
        div(class="container mt-2 d-flex flex-column align-items-center")
            a(class="btn btn-primary" href="/productos")| Listado